// import { useState } from "react";
// import { close, logo, menu } from "../assets";
import { navLinks } from "../constants/data";

export const Navbar = () => {
  return (
    <div className="navbar bg-base-100">
      <a className="btn btn-ghost text-xl">Pett Camping</a>
    </div>
  );
};
